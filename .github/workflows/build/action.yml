# ビルドして実行ファイルを作り出す

name: build

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: checkout
      uses: actions/checkout@v2

    - name: build
      run: go build
      working-directory: ./main

