on:
  push:
jobs:
  a:
    strategy:
      matrix:
        a: ${{ fromJson('["x"]') }}
    runs-on: ubuntu-latest
    steps:
      - run: exit 0
  b:
    needs:
      - a
    runs-on: ubuntu-latest
    steps:
      - run: exit 0
