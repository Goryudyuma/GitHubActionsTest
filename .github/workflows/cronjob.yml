name: Cron Job

on:
  schedule:
    - cron: '0/5 * * * *'

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: build
        uses: ./.github/actions/build
      - name: run
        uses: ./main